<%= simple_form_for @question do |f| %>
  <%= f.input :title, label: 'Başlık' %>
  <%= f.input :content, label: "İçerik",  placeholder: "İçeriği yazınız.." %>
  <%= f.association :tags,   as: :check_boxes %>
  <%= f.button :submit, "Kaydet" %>
<% end %>



